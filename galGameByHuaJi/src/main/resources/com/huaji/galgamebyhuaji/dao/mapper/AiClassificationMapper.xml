<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huaji.galgamebyhuaji.dao.AiClassificationMapper">
    <resultMap id="BaseResultMap" type="com.huaji.galgamebyhuaji.entity.AiClassification">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="client_id" jdbcType="BIGINT" property="clientId"/>
        <result column="gave_user" jdbcType="INTEGER" property="gaveUser"/>
        <result column="need_jurisdiction" jdbcType="INTEGER" property="needJurisdiction"/>
    </resultMap>
    <sql id="Example_Where_Clause">
        <where>
            <foreach collection="oredCriteria" item="criteria" separator="or">
                <if test="criteria.valid">
                    <trim prefix="(" prefixOverrides="and" suffix=")">
                        <foreach collection="criteria.criteria" item="criterion">
                            <choose>
                                <when test="criterion.noValue">
                                    and ${criterion.condition}
                                </when>
                                <when test="criterion.singleValue">
                                    and ${criterion.condition} #{criterion.value}
                                </when>
                                <when test="criterion.betweenValue">
                                    and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                                </when>
                                <when test="criterion.listValue">
                                    and ${criterion.condition}
                                    <foreach close=")" collection="criterion.value" item="listItem" open="("
                                             separator=",">
                                        #{listItem}
                                    </foreach>
                                </when>
                            </choose>
                        </foreach>
                    </trim>
                </if>
            </foreach>
        </where>
    </sql>
    <sql id="Update_By_Example_Where_Clause">
        <where>
            <foreach collection="example.oredCriteria" item="criteria" separator="or">
                <if test="criteria.valid">
                    <trim prefix="(" prefixOverrides="and" suffix=")">
                        <foreach collection="criteria.criteria" item="criterion">
                            <choose>
                                <when test="criterion.noValue">
                                    and ${criterion.condition}
                                </when>
                                <when test="criterion.singleValue">
                                    and ${criterion.condition} #{criterion.value}
                                </when>
                                <when test="criterion.betweenValue">
                                    and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                                </when>
                                <when test="criterion.listValue">
                                    and ${criterion.condition}
                                    <foreach close=")" collection="criterion.value" item="listItem" open="("
                                             separator=",">
                                        #{listItem}
                                    </foreach>
                                </when>
                            </choose>
                        </foreach>
                    </trim>
                </if>
            </foreach>
        </where>
    </sql>
    <sql id="Base_Column_List">
        id, client_id, gave_user, need_jurisdiction
    </sql>
    <select id="selectByExample" parameterType="com.huaji.galgamebyhuaji.entity.AiClassificationExample"
            resultMap="BaseResultMap">
        select
        <if test="distinct">
            distinct
        </if>
        <include refid="Base_Column_List"/>
        from ai_classification
        <if test="_parameter != null">
            <include refid="Example_Where_Clause"/>
        </if>
        <if test="orderByClause != null">
            order by ${orderByClause}
        </if>
    </select>
    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from ai_classification
        where id = #{id,jdbcType=BIGINT}
    </select>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete
        from ai_classification
        where id = #{id,jdbcType=BIGINT}
    </delete>
    <delete id="deleteByExample" parameterType="com.huaji.galgamebyhuaji.entity.AiClassificationExample">
        delete from ai_classification
        <if test="_parameter != null">
            <include refid="Example_Where_Clause"/>
        </if>
    </delete>
    <insert id="insert" parameterType="com.huaji.galgamebyhuaji.entity.AiClassification">
        insert into ai_classification (id, client_id, gave_user,
                                       need_jurisdiction)
        values (#{id,jdbcType=BIGINT}, #{clientId,jdbcType=BIGINT}, #{gaveUser,jdbcType=INTEGER},
                #{needJurisdiction,jdbcType=INTEGER})
    </insert>
    <insert id="insertSelective" parameterType="com.huaji.galgamebyhuaji.entity.AiClassification">
        insert into ai_classification
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="clientId != null">
                client_id,
            </if>
            <if test="gaveUser != null">
                gave_user,
            </if>
            <if test="needJurisdiction != null">
                need_jurisdiction,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id,jdbcType=BIGINT},
            </if>
            <if test="clientId != null">
                #{clientId,jdbcType=BIGINT},
            </if>
            <if test="gaveUser != null">
                #{gaveUser,jdbcType=INTEGER},
            </if>
            <if test="needJurisdiction != null">
                #{needJurisdiction,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>
    <select id="countByExample" parameterType="com.huaji.galgamebyhuaji.entity.AiClassificationExample"
            resultType="java.lang.Long">
        select count(*) from ai_classification
        <if test="_parameter != null">
            <include refid="Example_Where_Clause"/>
        </if>
    </select>
    <update id="updateByExampleSelective" parameterType="map">
        update ai_classification
        <set>
            <if test="row.id != null">
                id = #{row.id,jdbcType=BIGINT},
            </if>
            <if test="row.clientId != null">
                client_id = #{row.clientId,jdbcType=BIGINT},
            </if>
            <if test="row.gaveUser != null">
                gave_user = #{row.gaveUser,jdbcType=INTEGER},
            </if>
            <if test="row.needJurisdiction != null">
                need_jurisdiction = #{row.needJurisdiction,jdbcType=INTEGER},
            </if>
        </set>
        <if test="example != null">
            <include refid="Update_By_Example_Where_Clause"/>
        </if>
    </update>
    <update id="updateByExample" parameterType="map">
        update ai_classification
        set id = #{row.id,jdbcType=BIGINT},
        client_id = #{row.clientId,jdbcType=BIGINT},
        gave_user = #{row.gaveUser,jdbcType=INTEGER},
        need_jurisdiction = #{row.needJurisdiction,jdbcType=INTEGER}
        <if test="example != null">
            <include refid="Update_By_Example_Where_Clause"/>
        </if>
    </update>
    <update id="updateByPrimaryKeySelective" parameterType="com.huaji.galgamebyhuaji.entity.AiClassification">
        update ai_classification
        <set>
            <if test="clientId != null">
                client_id = #{clientId,jdbcType=BIGINT},
            </if>
            <if test="gaveUser != null">
                gave_user = #{gaveUser,jdbcType=INTEGER},
            </if>
            <if test="needJurisdiction != null">
                need_jurisdiction = #{needJurisdiction,jdbcType=INTEGER},
            </if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>
    <update id="updateByPrimaryKey" parameterType="com.huaji.galgamebyhuaji.entity.AiClassification">
        update ai_classification
        set client_id         = #{clientId,jdbcType=BIGINT},
            gave_user         = #{gaveUser,jdbcType=INTEGER},
            need_jurisdiction = #{needJurisdiction,jdbcType=INTEGER}
        where id = #{id,jdbcType=BIGINT}
    </update>
    <select id="getUserViewList" resultType="com.huaji.galgamebyhuaji.entity.AiClassification">
        select c.client_id
        from ai_classification as c
        join gal.users u on c.gave_user = u.user_id
        where gave_user = #{userId}
        or u.jurisdiction >=c.need_jurisdiction
    </select>
    <select id="userCanSee" resultType="java.lang.Integer">
        SELECT CASE
                   WHEN NOT EXISTS (SELECT 1
                                    FROM ai_classification
                                    WHERE client_id = #{configId}) THEN 0
                   WHEN EXISTS (SELECT 1
                                FROM ai_classification
                                WHERE client_id = #{configId}
                                  AND gave_user = #{userId}) THEN 1
                   ELSE (SELECT CASE WHEN u.jurisdiction >= COALESCE(acl.max_need, 0) THEN 1 ELSE 0 END
                         FROM gal.users u
                                  CROSS JOIN (SELECT MAX(need_jurisdiction) AS max_need
                                              FROM ai_classification
                                              WHERE client_id = #{configId}) acl
                         WHERE u.user_id = #{userId})
                   END AS result
    </select>
    <select id="getUserIdsByConfigId" resultType="java.lang.Integer">
        select gave_user
        from ai_classification
        where client_id=#{config}
    </select>

</mapper>
